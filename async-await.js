const { nextTick } = require("process");

const asyncc = async () => {
    
}

try {
        const user = await User.findOne(conditions);
            if (user) {
                
            }
} catch (error) {
    
}